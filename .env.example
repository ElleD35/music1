# Copy to .env and fill in. SERVER-SIDE ONLY — never prefix with NEXT_PUBLIC_.
SUNO_API_KEY=your_sunoapi_org_key_here

# Optional callback URL (we poll for status, so this is not required).
# SUNO_CALLBACK_URL=https://your-site.netlify.app/api/suno-callback
